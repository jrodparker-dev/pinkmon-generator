# Pokeball Picker assets

Add your separate pokeball image files to this folder and list each filename in `manifest.json`.

Example `manifest.json`:

```json
[
  "poke-ball.png",
  "great-ball.png",
  "ultra-ball.png"
]
```

Supported formats depend on browser support (PNG/WebP/JPG recommended).
